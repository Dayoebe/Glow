# Mobile App (React Native)

This folder is reserved for the React Native app.

Next step: decide on the tooling (React Native CLI for full native control, or Expo for faster iteration). Once chosen, we will scaffold the app here and connect it to the Laravel API.
